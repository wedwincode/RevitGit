<Window x:Class="RevitGit.UI.SaveVersion.SaveVersionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сохранить версию"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,6" Text="Комментарий:" />
        <TextBox x:Name="CommentTextBox"
                 Grid.Row="1"
                 MinHeight="72"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />
        <TextBlock Grid.Row="2"
                   Margin="0,7,0,16"
                   Foreground="DimGray"
                   Text="Комментарий необязателен." />
        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="92" Margin="0,0,8,0" IsDefault="True" Click="SaveClick">Сохранить</Button>
            <Button Width="92" IsCancel="True">Отмена</Button>
        </StackPanel>
    </Grid>
</Window>
